# 이미지 변환 및 이름 변경 프로그램 V2 사용 설명서

이 프로그램은 폴더 내의 이미지들을 불러와서 사용자가 원하는 순서와 이름으로 변경하고, 웹 최적화 이미지 포맷인 `WebP`로 일괄 변환해 주는 도구입니다. 이번 V2 업데이트를 통해 **미리보기, 실시간 변환명 확인, 상세한 변환 로그 확인** 기능이 대폭 추가되었습니다.

## 🚀 시작하기

1. **프로그램 실행:** `dist` 폴더 안에 생성된 `image_converter_ui.exe` 파일을 더블 클릭하여 실행합니다.

## 📖 기능별 사용 방법

### 1. 폴더 선택 및 이미지 불러오기 (좌측 상단)
* **[폴더 열기]** 버튼을 클릭하여 변환할 이미지가 들어있는 폴더를 선택합니다.
* 폴더를 선택하면 내부에 있는 이미지 목록이 리스트에 나타납니다.
* **선택 개수 표시:** 리스트 상단에서 현재 선택된 파일이 총 몇 개인지 확인할 수 있습니다.

### 2. 파일 선택 및 썸네일 미리보기 (좌측 & 우측 상단)
* **[전체 선택] / [선택 해제]** 버튼으로 파일들을 일괄 제어할 수 있습니다.
* 리스트에서 파일을 **클릭**하면, 우측 상단 큼직한 영역에 해당 이미지의 **미리보기(썸네일)** 와 원본 용량이 표시됩니다.
* **[▲ 위로 이동] / [▼ 아래로 이동]** 버튼을 사용하여 변환될 순서를 마음대로 바꿀 수 있습니다.

### 3. 변환 설정 (좌측 하단)
* **변경할 이름 (영문):** 파일의 기본이 될 이름을 입력합니다. 
  * 💡 *Tip:* `my profile image` 처럼 띄어쓰기를 입력하시면, 아래 "이름 연결 기호" 설정에 따라 `my_profile_image_1.webp` 처럼 자동으로 변환된 결과를 **[적용 예시]** 라벨에서 실시간으로 확인할 수 있습니다.
* **이름 연결 기호:** 이름과 숫자, 그리고 띄어쓰기 사이에 들어갈 기호를 선택합니다. (`_` 또는 `-`)
* **저장 위치:**
  * **현재 폴더:** 선택한 원본 폴더에 덮어쓰거나 함께 저장합니다.
  * **하위 폴더:** 특정 폴더를 새로 만들고 저장합니다. (예: `output`)
    * 💡 *옵션:* 하위 폴더 생성 시 앞에 날짜/시간을 자동으로 붙일 수 있습니다. ("년월일시", "년월일", "사용안함")
* **원본 파일:** 변환 완료 후 원본 파일을 삭제할지, 그대로 유지할지 선택합니다. 

### 4. 변환 실행 및 진행 상황 확인 (우측 하단)
* 모든 설정이 완료되면 좌측 맨 아래의 **[🚀 변환 실행]** 버튼을 클릭합니다.
* 우측 하단의 **진행 상태바(Progress Bar)** 를 통해 전체 진행도를 한눈에 확인할 수 있습니다.
* 넓은 로그 창을 통해 각 파일이 **얼마나 용량이 줄어들었는지**, 그리고 원본이 **삭제**되었는지 아주 상세하게 확인 가능합니다!

## ⚠️ 주의사항
* 원본 파일 삭제 기능을 사용할 때는 반드시 백업이나 확인 후 진행하시기 바랍니다. 삭제 후엔 휴지통으로 가지 않고 영구 삭제됩니다.
* 쾌적한 동작을 위해 한 번에 수백~수천 장을 변환하실 땐 여유를 가지고 기다려 주세요.
